주석처리에 대해 공부를 해보았다. 
아직 C언어밖에 하지않아서 주석처리가 씨언어에만 있는 건가? 생각했엇는데 자바도 주석처리가 있엇다.
//(한줄 주석), /* */(여러줄 주석하는거)으로 2가지 가있다. 
여러줄 주석처리하는 /* */가 자바를 하면서 가장 편리했다. 

또한 모든 언어의 기초인 출력하는 함수를 사용해서 "Hello java"를 했다. 씨언어에는 쓰고싶은 함수가 있으면 다써야하지만 자바에서는
System.out.println("");을 다안쳐도 되고 그냥 sysout만 치고 컨트롤+엔터만 치면되서 너무 편했다.  
